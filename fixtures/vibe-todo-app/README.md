# Vibe Todo fixture

Use this tiny repository to smoke-test discovery, architecture, implementation, QA, review, debug, and release workflows. It intentionally has no framework dependency so a host can inspect it without installation.
