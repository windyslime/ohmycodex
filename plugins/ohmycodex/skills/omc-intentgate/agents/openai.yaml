{
  "interface": {
    "display_name": "OhMyCodex IntentGate",
    "short_description": "Route intent through capabilities and safe continuation",
    "default_prompt": "Use $omc-intentgate to route this request and start safe continuation when needed."
  },
  "policy": {
    "allow_implicit_invocation": false
  }
}
